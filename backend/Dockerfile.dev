# Dockerfile for Backend Development
FROM node:20-slim

WORKDIR /app

# Copy package files first
COPY package*.json ./
RUN npm install

# Copy the rest of the code
COPY . .

# Expose Express port
EXPOSE 8080

# Start in dev mode with ts-node-dev for hot-reloading
CMD ["npm", "run", "dev"]
